# Ouroboros Configuration — Advanced Autonomy Enablement

pipeline:
  enabled: true
  auto_advance: true

task_graph:
  enabled: true

context:
  max_context_size: 160000 # Increased for multi-agent research
  enrich_context: true

llm:
  # Defer to ENV_VARS
  default_model: ""
  reasoning_effort: "high"